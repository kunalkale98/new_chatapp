version: 0.0
os: linux
files:
  - source: /
    destination: /new_chatapp
hooks:
  BeforeInstall:
    - location: scripts/clean-up.sh
      timeout: 300
      runas: root
  ApplicationStart:
    - location: scripts/start-server.sh
      timeout: 300
      runas: root
